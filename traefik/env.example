# Traefik Environment Configuration

# Base domain for services
DOMAIN=example.com

# Let's Encrypt email
ACME_EMAIL=admin@example.com

# Dashboard authentication (generate with: htpasswd -nb admin password)
# DASHBOARD_AUTH=admin:$2y$10$...

# IP whitelist for dashboard (comma separated)
# DASHBOARD_WHITELIST=127.0.0.1/32,192.168.1.0/24

# Cloudflare API token (if using DNS challenge)
# CF_API_TOKEN=your-cloudflare-api-token

# Traefik Pilot token (optional)
# PILOT_TOKEN=your-pilot-token
